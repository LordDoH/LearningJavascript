// Numbers
var totalUsers
console.log(camels)
var totalUsers = 100
var averageAge = 30.5

// console.log(camels)
let camels
camels = 1 // undefined
const camel = 'Hola'

// console.log(camels)

const numero = 5

// BigInt
// Max safe integer in JavaScript is 2^53 - 1
const bigInt = 1234567890123456789012345678901234567890n

// Inexact division from javascript
const inexactResult = 0.1 + 0.2 // 0.30000000000000004
Math.round()
const exactResult = (0.1 * 10 + 0.2 * 10) / 10 // 0.3

const pow = Math.pow(2, 4) // 16
Math.floor(1.9) // 1
Math.ceil(1.1)
Math.max(1, 2, 3, 4, 5) // 5
// console.log(Math.random())

Math.trunc(1.9) // 1
Math.PI // 3.141592653589793

let wheels = 4

wheels = wheels + 1

function addWheels() {
  if (wheels === 4) {
    var llantas = 4
  }
  const numero = 8
  console.log(llantas)
  console.log(numero)
  var totalUsers = 200
}

console.log(numero)

// addWheels()

// Var
var totalUsers = 200
totalUsers = 200

// Let
wheels = 8

// Const
numero = 8
console.log(numero)
